# V7.6 工具选择优化总结

**版本**: V7.6  
**日期**: 2026-01-22  
**优化主题**: Schema > Plan > Intent 三级优先级 + 健壮性 + 可观测性 + 可调试性

---

## 一、优化背景

### 原有设计评估

**当前设计**（V7.5）：
- ✅ 三级优先级逻辑清晰（Schema > Plan > Intent）
- ✅ 配置优先，符合工程实践
- ✅ 有兜底机制

**存在问题**：
- ❌ Schema 配置无效工具时，缺乏验证和告警
- ❌ Schema 覆盖 Plan/Intent 时，缺乏透明度
- ❌ Tracer 缺少完整的决策上下文

### 优化目标

**保持原有设计的优点**，补充三个关键能力：
1. 🔧 **健壮性**：自动检测并过滤无效工具
2. 👁️ **可观测性**：记录覆盖关系，决策过程透明
3. 🐛 **可调试性**：完整追踪，支持问题回溯

---

## 二、优化实现

### 优化 1: Schema 工具有效性验证

**实现位置**: `core/agent/simple/simple_agent.py` L806-819

```python
if self.schema.tools:
    valid_tools = []
    invalid_tools = []
    for tool_name in self.schema.tools:
        if self.capability_registry.get(tool_name) or tool_name in self.tool_selector.NATIVE_TOOLS:
            valid_tools.append(tool_name)
        else:
            invalid_tools.append(tool_name)
    
    if invalid_tools:
        logger.warning(
            f"⚠️ Schema 配置了无效工具: {invalid_tools}，已自动过滤。"
            f"有效工具: {valid_tools}"
        )
    required_capabilities = valid_tools
```

**效果**：
- ✅ 自动过滤无效工具，避免运行时错误
- ✅ 清晰的警告日志，方便运营排查配置问题
- ✅ 显示可用工具列表，辅助运营修正配置

---

### 优化 2: 覆盖透明化日志

**实现位置**: `core/agent/simple/simple_agent.py` L824-832, L916-922

```python
if not use_skill_path:
    selection_source = "schema"
    # 记录被覆盖的内容
    if plan_capabilities:
        overridden_sources.append(f"plan:{plan_capabilities[:3]}")
    if intent_capabilities:
        overridden_sources.append(f"intent:{intent_capabilities[:3]}")
    if overridden_sources:
        logger.info(
            f"📋 Schema 工具优先: {valid_tools}，覆盖了 {overridden_sources}"
        )
```

**效果**：
- ✅ 明确记录哪些建议被覆盖，增强决策透明度
- ✅ 方便评估 Schema 配置是否合理
- ✅ 辅助调试：当任务失败时，可快速定位是否因工具缺失

**示例日志**：
```
📋 Schema 工具优先: ['web_search']，覆盖了 ["plan:['e2b_sandbox', 'ppt_generator']", "intent:['e2b_sandbox', 'ppt_generator']"]
```

---

### 优化 3: Tracer 增强追踪

**实现位置**: `core/agent/simple/simple_agent.py` L924-941

```python
if self._tracer:
    tool_stage.set_input({
        "schema_tools": self.schema.tools if self.schema.tools else [],
        "plan_capabilities": plan_capabilities[:5] if plan_capabilities else [],
        "intent_capabilities": intent_capabilities[:5] if intent_capabilities else [],
        "selection_source": selection_source,
        "use_skill_path": use_skill_path
    })
    tool_stage.complete({
        "tool_count": len(selection.tool_names),
        "tools": selection.tool_names[:8],
        "base_tools": selection.base_tools,
        "dynamic_tools": selection.dynamic_tools[:5],
        "overridden_sources": overridden_sources,
        "invocation_type": invocation_strategy.type.value if invocation_strategy else "skill",
        "final_source": selection_source
    })
```

**效果**：
- ✅ 完整记录三层建议（Schema/Plan/Intent）
- ✅ 记录最终选择来源和理由
- ✅ 记录是否发生覆盖，以及覆盖的内容
- ✅ 支持离线分析：导出 Tracer 数据后可批量分析工具选择模式

---

## 三、测试验证

### 测试文件

**位置**: `tests/test_tool_selection_v76.py`

### 测试用例

1. **测试 Schema 无效工具过滤**
   - 配置：`tools: ["web_search", "invalid_tool_123", "another_invalid"]`
   - 验证：自动过滤无效工具 + 警告日志

2. **测试覆盖透明化日志**
   - 配置：Schema 只配置 `["web_search"]`，Plan 建议 `["e2b_sandbox", "ppt_generator"]`
   - 验证：日志记录覆盖关系

3. **测试 Tracer 增强追踪**
   - 配置：启用 Tracer
   - 验证：Tracer 记录包含三层建议 + 覆盖信息 + 选择理由

### 测试结果

```bash
============================================================
V7.6 优化验证
Schema > Plan > Intent 优先级 + 有效性验证 + 透明化日志
============================================================

✅ PASS - Schema 无效工具过滤
✅ PASS - 覆盖透明化日志
✅ PASS - Tracer 增强追踪

通过率: 3/3 (100%)
```

---

## 四、架构文档更新

### 更新内容

1. **文档标题**: `V7.5` → `V7.6`
2. **架构状态**: 添加"工具选择优化 V7.6"说明
3. **版本概述**: 新增 V7.6 核心特性章节
4. **流程图**: 阶段 3.2 增加 V7.6 优化说明
5. **详细章节**: 新增"V7.6: 工具选择三级优化"独立章节
6. **版本演进表**: 添加 V7.6 记录
7. **目录**: 更新所有相关索引

### 文档位置

`docs/architecture/00-ARCHITECTURE-OVERVIEW.md`

---

## 五、优化总结

| 优化项 | 优化前 | 优化后 | 价值 |
|--------|--------|--------|------|
| **Schema 验证** | 无效工具导致运行时错误 | 自动过滤 + 警告日志 | 🔧 健壮性提升 |
| **覆盖透明化** | 覆盖逻辑不可见 | 明确记录覆盖内容 | 👁️ 可观测性提升 |
| **Tracer 增强** | 只记录最终结果 | 记录完整决策过程 | 🐛 可调试性提升 |

### 关键价值

1. **运营友好**：配置错误时有明确提示，降低试错成本
2. **调试高效**：完整的决策链路，快速定位问题
3. **分析支持**：Tracer 数据支持离线批量分析工具选择模式
4. **向后兼容**：优化不改变现有逻辑，只是增强日志和追踪

---

## 六、后续建议

### 短期（已完成）

- ✅ Schema 工具有效性验证
- ✅ 覆盖透明化日志
- ✅ Tracer 增强追踪
- ✅ 测试覆盖
- ✅ 架构文档更新

### 中期（可选）

- 🔄 **工具推荐置信度**：引入置信度评分，低置信度时触发人工审核
- 🔄 **智能建议**：当检测到无效工具时，推荐相似的有效工具
- 🔄 **配置向导**：提供交互式工具配置向导，辅助运营配置

### 长期（可选）

- 🔄 **自适应策略**：根据历史成功率，动态调整工具选择策略
- 🔄 **A/B 测试**：支持工具配置的 A/B 测试，验证配置优化效果

---

## 七、相关文件清单

### 代码文件

- `core/agent/simple/simple_agent.py` (L780-943)
- `core/tool/selector.py`
- `core/tool/capability/registry.py`

### 测试文件

- `tests/test_tool_selection_v76.py`

### 文档文件

- `docs/architecture/00-ARCHITECTURE-OVERVIEW.md`
- `V7.6_OPTIMIZATION_SUMMARY.md` (本文件)

---

**优化完成时间**: 2026-01-22  
**优化版本**: V7.6  
**测试状态**: ✅ 全部通过 (3/3)
