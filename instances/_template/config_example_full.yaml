# ==================== 完整配置示例 ====================
#
# 这是一个完整的实例配置示例
# 启用所有可用工具，适合复杂任务
#
# 使用场景：
# - 需要多种工具协同的复杂任务
# - 文档生成、数据处理、代码执行等
# - 完整功能验证

# ==================== 实例基本信息 ====================
instance:
  name: full-featured-agent
  description: "完整功能的智能体，包含所有可用工具"
  version: 1.0.0

# ==================== Agent 配置 ====================
agent:
  model: claude-sonnet-4-20250514
  max_turns: 30
  plan_manager_enabled: true
  allow_parallel_tools: true

# ==================== LLM 超参数 ====================
llm_params:
  temperature: 0.7
  max_tokens: 8000

# ==================== 通用工具能力配置 ====================
enabled_capabilities:
  # --- 内容生成类 (Claude Skills) ---
  pptx: 1                    # PPT 生成
  xlsx: 1                    # Excel 表格操作
  docx: 1                    # Word 文档生成
  pdf: 1                     # PDF 生成
  
  # --- 信息获取类 ---
  web_search: 1              # 互联网搜索
  exa_search: 1              # Exa 语义搜索
  knowledge_search: 1        # 个人知识库检索
  
  # --- 数据处理类（沙盒工具）---
  sandbox_list_dir: 1        # 沙盒列目录
  sandbox_read_file: 1       # 沙盒读文件
  sandbox_write_file: 1      # 沙盒写文件
  sandbox_delete_file: 1     # 沙盒删文件
  sandbox_file_exists: 1     # 沙盒检查文件存在
  sandbox_run_command: 1     # 沙盒运行命令
  sandbox_run_code: 1        # 沙盒代码执行
  sandbox_create_project: 1  # 沙盒项目创建
  sandbox_run_project: 1     # 沙盒项目运行
  
  # --- PPT 生成类 ---
  ppt_generator: 1           # 闭环 PPT 生成工具
  slidespeak_render: 1       # SlideSpeak 渲染工具
  
  # --- 核心工具 ---
  plan_todo: 1               # 任务规划工具
  api_calling: 1             # 通用 API 调用
  request_human_confirmation: 1  # 人工确认
  file_read: 1               # 文件读取
  code_execution: 1          # 动态代码执行

# ==================== MCP 工具配置 ====================
mcp_tools:
  # 示例：Dify 工作流
  - name: dify_flowchart
    server_url: "https://api.dify.ai/mcp/server/xxx/mcp"
    server_name: "dify"
    auth_type: "bearer"
    auth_env: "DIFY_API_KEY"
    description: "生成 flowchart 流程图"
    capability: "chart_generation"

# ==================== REST APIs 配置 ====================
apis:
  # 示例：天气 API
  - name: weather_api
    base_url: "https://api.weather.com/v1"
    auth:
      type: api_key
      header: X-API-Key
      env: WEATHER_API_KEY
    doc: weather
    capability: "web_search"
    description: "天气查询服务"

# ==================== 记忆配置 ====================
memory:
  mem0_enabled: true
  smart_retrieval: true
  retention_policy: user

# ==================== Multi-Agent 配置（V6.0）====================
multi_agent:
  # mode 选项：disabled / auto / enabled
  mode: "auto"
  max_parallel_workers: 5
  execution_strategy: "auto"
  
  # 自动触发配置（mode=auto 时生效）
  auto_trigger_keywords:
    - "研究"
    - "分析"
    - "对比"
    - "调研"
  auto_trigger_min_complexity: "complex"

# ==================== 高级配置 ====================
advanced:
  intent_analyzer:
    enabled: true
    use_llm: true
  
  output_formatter:
    default_format: "markdown"
    code_highlighting: true
