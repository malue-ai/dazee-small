# ============================================================
# SAP 创作助手 实例配置
# ============================================================
# 
# Agent ID: SAP_xiaodazi
# 从 Protocol 自动生成符合 ICH E9(R1) 的统计分析计划（SAP），支持实体抽取、内容生成、代码生成和合规检查
#
# ============================================================

instance:
  name: SAP 创作助手
  description: 从 Protocol 自动生成符合 ICH E9(R1) 的统计分析计划（SAP），支持实体抽取、内容生成、代码生成和合规检查
  version: 1.0.0
agent:
  provider: qwen
  model: qwen3-max
