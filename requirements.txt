# ============================================================
# 运行时依赖（打包构建使用此文件）
#
# 开发/测试工具 → requirements-dev.txt
# 可选重量级依赖 → requirements-optional.txt
# ============================================================

# FastAPI 核心依赖
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.3,<3.0.0

# Claude API
anthropic>=0.40.0

# OpenAI SDK（Responses API 需要 >=2.20.0）
openai>=2.20.0

# 配置管理
pyyaml>=6.0
python-dotenv>=1.0.0      # .env 文件加载（实例 API Key 等）

# 网络 / 异步 I/O
python-multipart>=0.0.6
aiofiles>=24.1.0          # 异步文件操作
aiohttp>=3.9.0            # 异步 HTTP 客户端（tools/api_calling.py）
httpx>=0.27.0             # HTTP 客户端
filetype>=1.2.0           # 文件类型检测（纯 Python，无需系统依赖）

# 本地数据库（SQLite）
aiosqlite>=0.20.0         # SQLite 异步驱动
greenlet>=3.0.0           # SQLAlchemy 异步支持
sqlalchemy[asyncio]>=2.0.0

# Token 计数
tiktoken>=0.5.0

# 向量计算（意图缓存余弦相似度）
numpy>=1.26.0

# JSON 解析（宽松模式）
json5>=0.9.0
json-repair>=0.44.0

# Mem0 记忆系统
mem0ai>=0.1.29            # 向量记忆核心（长期记忆存储+检索）
sqlite-vec>=0.1.1         # SQLite 向量搜索扩展（本地向量存储）
huggingface-hub>=0.20.0   # HuggingFace 模型下载（嵌入模型 GGUF）
llama-cpp-python>=0.2.0   # GGUF 本地 Embedding（BGE-M3 Q4）

# 文件处理
Pillow>=10.0.0            # 图片压缩
PyPDF2>=3.0.0             # PDF 文本提取（旧版 fallback）
pypdf>=4.0.0              # PDF 文本提取（新版，知识库索引首选）
pdfplumber>=0.11.0        # PDF 表格+文本提取（本地降级方案）
python-docx>=0.8.11       # Word 文档解析（页数估算）
python-pptx>=0.6.21       # PPT 生成和解析
openpyxl>=3.0.0           # Excel 表格解析（工作表数量）
unstructured-client>=0.30.6  # 非结构化数据处理客户端

# 定时任务
apscheduler>=3.10.0
croniter>=6.0.0           # Cron 表达式解析

# Google Gemini API（图像生成）
google-genai>=1.0.0       # Gemini 3 Pro Image API

# macOS 本地 OCR（Vision framework）
pyobjc-core>=12.0; sys_platform == "darwin"
pyobjc-framework-Vision>=12.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=12.0; sys_platform == "darwin"

# 浏览器自动化（browser tool 基础依赖，二进制由首次使用时自动安装）
playwright>=1.49.0

# 系统监控
psutil>=5.9.0

# WebSocket（实时通信）
websockets>=12.0

# Multi-channel gateway
python-telegram-bot>=21.0     # Telegram bot long polling
lark-oapi>=1.3.0              # Feishu (Lark) WebSocket + API
python-socks[asyncio]>=2.0    # SOCKS proxy support for WebSocket
