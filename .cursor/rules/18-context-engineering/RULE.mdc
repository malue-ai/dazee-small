---
description: 上下文工程极限优化规范。适用于所有涉及 LLM 上下文构建、多模态数据处理、工具结果处理、Skill/Agent 设计的场景。
globs:
  - core/context/**
  - core/agent/**
  - core/prompt/**
  - core/memory/**
  - core/knowledge/**
  - utils/file_processor.py
  - utils/file_handler.py
  - tools/**
  - skills/**
  - instances/*/skills/**
  - prompts/**
alwaysApply: true
---

# 上下文工程极限优化规范（强制）

## 核心哲学：文件为媒介，文字交换为主

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                                                                 │
│  🧠 上下文窗口 = 最昂贵的认知空间，每一个 token 都必须「值得」                    │
│                                                                                 │
│  ┌──────────────────────────────────────────────────────────────────────────┐    │
│  │                                                                          │    │
│  │  原则 1: 上下文只装「决策所需的最小信息」                                  │    │
│  │  原则 2: 长内容/多模态数据通过临时文件交换，代码负责读写                    │    │
│  │  原则 3: 优先文字摘要，而非原始数据                                        │    │
│  │  原则 4: 分层加载 — 先摘要，按需展开                                       │    │
│  │  原则 5: 已完成子任务的中间过程可折叠，只保留结论                            │    │
│  │                                                                          │    │
│  └──────────────────────────────────────────────────────────────────────────┘    │
│                                                                                 │
│  ❌ 把 50KB 文本全塞进 user message                                              │
│  ❌ 把完整搜索结果 JSON 放进 tool_result                                          │
│  ❌ 把图片 base64 和原始像素数据留在历史消息                                       │
│  ❌ 把前 20 轮工具调用的完整输入输出保留在上下文                                    │
│                                                                                 │
│  ✅ 50KB 文本 → 写入临时文件 → 上下文只放「文件路径 + 500 字摘要」                 │
│  ✅ 搜索结果 → 代码提取 top-5 条目 → 上下文只放标题+摘要                           │
│  ✅ 图片 → URL 引用 + 200 字文字描述（如果已经处理过）                              │
│  ✅ 旧轮次工具结果 → 折叠为「工具名 + 结论一句话」                                  │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 一、数据分层模型（强制）

所有进入 LLM 上下文的数据必须按以下三层分类处理：

```
┌─────────────────────────────────────────────────┐
│  Layer 0: 上下文层（Context Window）              │
│  ── 直接出现在 messages 中 ──                     │
│  • 系统提示词（缓存稳定）                         │
│  • 用户当前查询                                   │
│  • 摘要/结论（≤ 500 tokens per item）             │
│  • 文件路径引用                                   │
│  • 当前计划状态                                   │
│  • 最近 1-2 轮工具结果（完整）                     │
│  目标：总量控制在 token_budget × 70% 以内          │
├─────────────────────────────────────────────────┤
│  Layer 1: 工作文件层（Scratchpad Files）           │
│  ── 临时文件，Agent 可按需读取 ──                  │
│  • 工具完整输出（JSON/文本）                       │
│  • 下载的文档/网页内容                             │
│  • 中间计算结果                                   │
│  • 搜索结果原始数据                               │
│  存储位置：workspace/scratchpad/{session_id}/     │
│  生命周期：会话结束后可清理                        │
├─────────────────────────────────────────────────┤
│  Layer 2: 持久存储层（Persistent Storage）         │
│  ── 数据库/文件系统，跨会话持久 ──                  │
│  • 知识库索引（FTS5/向量）                         │
│  • 用户记忆（MEMORY.md）                           │
│  • Playbook 策略库                                │
│  • 历史对话存档                                   │
│  存储位置：~/.xiaodazi/data/                      │
└─────────────────────────────────────────────────┘
```

---

## 二、多模态数据处理（强制）

### 总原则：多模态 → 文字描述 + 文件引用

| 数据类型 | 上下文内放什么 | 完整数据存哪里 |
|---|---|---|
| **图片** | URL 引用（Claude 原生支持）；历史轮次图片 → 替换为文字描述 | 原始 URL / 本地缓存 |
| **视频** | 文字描述（时长/内容概要）+ 文件路径 | 本地文件 / URL |
| **音频** | 转录文字摘要 + 文件路径 | 本地文件 |
| **PDF/文档** | 首段摘要（≤ 500字）+ 文件路径 + 页数/大小 | 本地文件 / URL |
| **表格/CSV** | 列名 + 前 5 行 + 统计摘要 + 文件路径 | 本地文件 |
| **代码文件** | 核心函数签名 + 文件路径 + 行数 | 本地文件 |
| **网页** | 标题 + 核心段落摘要 + URL | scratchpad 文件 |

### 图片处理规则

```python
# ✅ 正确：当前轮次图片使用 URL 引用（Claude 原生支持，高效）
content_block = {"type": "image", "source": {"type": "url", "url": image_url}}

# ✅ 正确：历史轮次（> 2 轮前）的图片替换为文字描述
# _strip_old_images() 已实现，但替换内容应更有价值
old_image_replacement = {
    "type": "text",
    "text": f"[图片: {filename}, 用户在第 {turn} 轮上传，内容为{description}]"
}

# ❌ 错误：所有历史图片保留 base64/URL
# ❌ 错误：图片不做任何描述就直接丢弃
```

### 文本文件处理规则（关键优化点）

```python
# ❌ 错误：50KB 文本全部塞进消息
text_content = await download_file(url)  # 50KB
message = f"文件内容：\n{text_content}"  # 全部塞进上下文

# ✅ 正确：大文本写入 scratchpad，上下文只放摘要 + 路径
async def process_large_text(content: str, filename: str, session_id: str) -> str:
    """处理大文本文件：写入 scratchpad，返回上下文摘要"""
    if len(content) <= 2000:  # ≤ 2000 字符直接放入上下文
        return content
    
    # 写入 scratchpad 文件
    scratchpad_path = f"workspace/scratchpad/{session_id}/{filename}"
    await write_scratchpad(scratchpad_path, content)
    
    # 生成上下文摘要
    preview = content[:500]  # 前 500 字符预览
    return (
        f"📄 文件: {filename} ({len(content)} 字符)\n"
        f"完整内容: {scratchpad_path}\n"
        f"预览:\n{preview}\n...\n"
        f"💡 使用 cat {scratchpad_path} 查看完整内容"
    )
```

**文本大小分级处理**：

| 文本大小 | 处理方式 |
|---|---|
| ≤ 2KB | 直接放入上下文（约 500 tokens） |
| 2KB - 10KB | 前 500 字 + 尾 200 字 + scratchpad 文件路径 |
| 10KB - 50KB | LLM 生成摘要（≤ 500 字）+ scratchpad 文件路径 |
| > 50KB | 仅文件路径 + 元数据（大小/类型/行数），Agent 按需 cat 片段 |

### 搜索结果处理规则

```python
# ❌ 错误：完整搜索结果 JSON 直接返回
tool_result = json.dumps(search_response)  # 可能 10000+ 字符

# ✅ 正确：代码层提取关键信息，上下文只放精华
def extract_search_summary(raw_results: List[Dict], top_k: int = 5) -> str:
    """从搜索结果提取精华摘要"""
    summaries = []
    for item in raw_results[:top_k]:
        summaries.append(
            f"- [{item['title']}]({item['url']})\n"
            f"  {item['snippet'][:200]}"
        )
    
    # 完整结果写入 scratchpad
    save_to_scratchpad(raw_results)
    
    return f"搜索到 {len(raw_results)} 条结果，Top {top_k}:\n" + "\n".join(summaries)
```

---

## 三、工具结果压缩策略（强制）

### 即时压缩（当前轮次）

工具执行完毕后，立即决定压缩策略：

| 工具类型 | 压缩策略 | 上下文保留 |
|---|---|---|
| 搜索类（web_search/exa） | 提取 top-5 摘要 | ≤ 2000 字符 |
| 文件读取（cat/read） | 保留首尾 + scratchpad | ≤ 1000 字符 |
| 代码执行 | stdout/stderr 前 50 行 + scratchpad | ≤ 1500 字符 |
| API 调用 | 关键字段提取 | ≤ 1000 字符 |
| 列表/枚举 | 前 10 条 + 总数 | ≤ 500 字符 |

### 历史轮次折叠（Context Folding）

已完成的工具调用链，折叠为一句话结论：

```python
# ✅ 正确：旧轮次 tool_result 折叠
# 原始（3 轮前）：
#   tool_use: web_search("Python 排序算法")
#   tool_result: {"results": [...5000 字符...]}
#
# 折叠后：
#   "[第 3 轮] web_search → 找到 8 条结果，最相关：快速排序 O(nlogn)、归并排序、TimSort"

def fold_old_tool_result(tool_name: str, result: str, turn: int) -> str:
    """将旧轮次工具结果折叠为一句话"""
    # 方案 A：代码规则提取（≤ 100 字符结论）
    if len(result) <= 200:
        return result  # 短结果不折叠
    
    conclusion = extract_conclusion(result)  # 代码提取结论
    return f"[第{turn}轮] {tool_name} → {conclusion}"
```

### 压缩阈值（调优后）

```python
# 当前轮次：完整保留
CURRENT_TURN_THRESHOLD = float('inf')

# 最近 2 轮：适度压缩（保留头尾 + scratchpad 引用）
RECENT_TURNS_THRESHOLD = 3000  # 字符

# 更早轮次：激进压缩（仅保留结论）
OLD_TURNS_THRESHOLD = 200  # 字符
```

---

## 四、Scratchpad 文件交换机制（强制）

### 设计原则

Scratchpad 是 LLM 上下文和大数据之间的「缓冲区」：

```
┌──────────┐     写入摘要      ┌──────────────┐     按需 cat      ┌──────────────┐
│  Tool     │ ──────────────→ │  LLM Context  │ ──────────────→  │  Scratchpad   │
│  执行结果  │     (≤500字)    │  (精简高效)    │   (需要详情时)    │  Files        │
│  (10KB+)  │                 │               │                  │  (完整数据)    │
└──────────┘                  └──────────────┘                   └──────────────┘
     │                                                                 ↑
     └─────────────── 完整数据写入 ──────────────────────────────────────┘
```

### 目录结构

```
workspace/
├── scratchpad/
│   └── {session_id}/
│       ├── search_001.json        # 搜索结果完整数据
│       ├── file_content_002.txt   # 下载的文件内容
│       ├── api_response_003.json  # API 调用完整响应
│       └── _index.json            # 文件索引（名称→用途映射）
│
├── storage/
│   └── tool_results/              # 已有的工具结果压缩存储
│       └── {ref_id}.json
```

### 使用规范

```python
# ✅ Skill 开发规范：大数据必须走 scratchpad
async def my_skill_execute(params: dict, context: RuntimeContext) -> str:
    result = await heavy_operation()
    
    if len(str(result)) > 2000:
        # 写入 scratchpad
        path = await context.write_scratchpad(
            filename="result.json",
            content=json.dumps(result, ensure_ascii=False)
        )
        # 返回摘要 + 路径
        return (
            f"处理完成，共 {len(result)} 条数据。\n"
            f"关键发现：{extract_key_findings(result)}\n"
            f"完整数据：{path}"
        )
    
    return str(result)
```

---

## 五、上下文注入预算控制（强制）

### 每类注入源的 Token 预算

| 注入源 | 最大 Token | 说明 |
|---|---|---|
| 系统角色提示词 | 2000 | 缓存稳定，一次性开销 |
| 工具定义 | 3000 | 缓存稳定 |
| 记忆召回（Memory） | 500 | 仅 top-3 条最相关记忆 |
| Playbook 策略 | 300 | 仅最匹配的 1 条策略摘要 |
| 知识检索（Knowledge） | 800 | top-3 snippet + 引用路径 |
| 历史摘要 | 500 | 压缩后的对话历史 |
| Plan/Todo 状态 | 300 | 当前目标 + 进度 |
| Skill 提示词（动态） | 1000 | 当前激活 Skill 的指令 |
| **单个 tool_result** | **3000** | **超出写 scratchpad** |
| **合计（非消息部分）** | **≤ 12000** | **约占 200K 预算的 6%** |

### Injector 必须遵守预算

```python
# ✅ 正确：Injector 内部截断到预算
class UserMemoryInjector:
    MAX_TOKENS = 500
    
    async def inject(self, context) -> str:
        memories = await recall(query, top_k=5)
        # 从最相关的开始，累计 token 直到预算
        result = []
        token_count = 0
        for mem in memories:
            mem_tokens = count_tokens(mem.content)
            if token_count + mem_tokens > self.MAX_TOKENS:
                break
            result.append(mem.content)
            token_count += mem_tokens
        return "\n".join(result)

# ❌ 错误：Injector 不控制输出大小
class BadInjector:
    async def inject(self, context) -> str:
        return await get_all_memories()  # 可能返回 5000 tokens
```

---

## 六、Skill 设计中的上下文规范（强制）

所有 Skill 必须遵守以下上下文纪律：

### 6.1 输入处理

```yaml
# SKILL.md 中必须声明输入处理策略
input_handling:
  max_context_input: 2000  # Skill 从上下文接收的最大字符数
  large_input_strategy: "scratchpad"  # file / scratchpad / chunked
  supported_formats:
    - text: "inline"        # ≤ 2KB 直接接收
    - image: "url_ref"      # URL 引用
    - document: "file_path" # 文件路径
```

### 6.2 输出处理

```yaml
# SKILL.md 中必须声明输出处理策略
output_handling:
  max_context_output: 3000  # Skill 返回到上下文的最大字符数
  large_output_strategy: "scratchpad_with_summary"
  # 可选策略：
  # - "truncate": 截断到 max_context_output
  # - "scratchpad_with_summary": 完整数据写 scratchpad，返回摘要
  # - "streaming": 流式分块返回
```

### 6.3 Skill 输出示例

```python
# ✅ 正确：Skill 输出控制
class ExcelAnalyzerSkill:
    async def execute(self, file_path: str) -> str:
        df = pd.read_csv(file_path)
        
        # 完整分析结果写 scratchpad
        full_analysis = self.analyze(df)
        scratchpad_path = await self.save_scratchpad(full_analysis)
        
        # 上下文只返回摘要
        return (
            f"## Excel 分析完成\n"
            f"- 行数: {len(df)}, 列数: {len(df.columns)}\n"
            f"- 列: {', '.join(df.columns[:10])}\n"
            f"- 关键统计:\n{df.describe().to_string()[:500]}\n"
            f"\n完整分析: {scratchpad_path}"
        )

# ❌ 错误：Skill 返回完整 DataFrame
class BadExcelSkill:
    async def execute(self, file_path: str) -> str:
        df = pd.read_csv(file_path)
        return df.to_string()  # 可能 100KB+
```

---

## 七、对话历史压缩策略（增强）

### 7.1 渐进式消息衰减

```
时间轴: ←────────────────────────────────→ 当前
消息:    M1  M2  M3  M4 ... M15  M16  M17  M18  M19  M20

处理:    [────── 摘要区 ──────]  [── 折叠区 ──]  [── 完整区 ──]
         LLM 摘要(≤500 tokens)  头尾压缩        原文保留

         M1-M10 → 一段摘要      M11-M16 折叠    M17-M20 完整
                                 tool_result
                                 → 一句话结论
```

### 7.2 图片自动衰减（已实现，需增强）

```python
# 当前：_strip_old_images() 替换为 "[图片已省略]"
# 优化：替换为有意义的文字描述

async def decay_old_image(image_block: dict, turn: int) -> dict:
    """图片衰减：URL → 文字描述"""
    # 如果之前 LLM 已经描述过这张图片（在 assistant 回复中），
    # 提取那段描述作为替换文本
    description = extract_image_description_from_history(turn)
    
    if description:
        return {"type": "text", "text": f"[图片@第{turn}轮: {description}]"}
    
    return {"type": "text", "text": f"[图片@第{turn}轮: 用户上传的图片]"}
```

### 7.3 工具对（tool_use + tool_result）折叠

```python
# 超过 N 轮的工具对，折叠为单行摘要
def fold_tool_pair(tool_use: dict, tool_result: dict, turn: int) -> str:
    tool_name = tool_use.get("name", "unknown")
    
    # 提取 tool_result 的结论
    result_text = extract_text(tool_result)
    if len(result_text) > 150:
        conclusion = result_text[:100] + "..."
    else:
        conclusion = result_text
    
    return f"[T{turn}] {tool_name} → {conclusion}"
```

---

## 八、行业最佳实践融合

### 8.1 Context Folding（上下文折叠）

借鉴 AgentFold 论文（2025），实现子任务完成后自动折叠：

```
任务: 调研竞品并写报告

执行过程:
  Branch 1: 搜索竞品 A
    ├─ web_search("竞品A 功能") → 8 条结果 (5000 tokens)
    ├─ web_search("竞品A 定价") → 5 条结果 (3000 tokens)
    └─ Return: "竞品A: SaaS模式, 核心功能X/Y/Z, 定价$99/月" (50 tokens)
                                                          ↑ 折叠后仅保留这一行
  Branch 2: 搜索竞品 B
    └─ Return: "竞品B: 开源模式, 功能P/Q, 免费" (30 tokens)

  Main: 写报告（基于折叠后的结论）
```

**效果**：原始上下文 8000+ tokens → 折叠后 80 tokens（100x 压缩）

### 8.2 Cognitive Workspace（认知工作区）

借鉴 Cognitive Workspace 论文（2025），实现主动记忆管理：

```python
# Agent 主动决定哪些信息值得保留在上下文
# 通过 System Prompt 指导：
MEMORY_GUIDANCE = """
你有一个 scratchpad 工作区用于存储中间结果。
当工具返回大量数据时：
1. 提取关键发现写入你的回复
2. 完整数据已自动保存到 scratchpad
3. 需要详情时使用 cat 命令查看 scratchpad 文件
"""
```

### 8.3 Acon（Agent Context Optimization）

借鉴 Acon 论文（2025），对环境观测和交互历史统一压缩：

- **环境观测压缩**：工具结果、搜索结果、文件内容 → 提取决策相关信息
- **交互历史压缩**：旧轮次 → 摘要；失败轮次 → 保留教训，丢弃细节
- **自适应压缩**：根据任务复杂度动态调整压缩阈值

---

## 九、禁止模式

### ❌ 禁止：大数据直塞上下文

```python
# ❌ 严禁：文件内容直接塞进消息
content = await read_file(path)  # 可能 100KB
messages.append({"role": "user", "content": f"文件内容:\n{content}"})

# ❌ 严禁：API 响应不做处理直接返回
result = await api_call()
return json.dumps(result)  # 可能 20KB JSON

# ❌ 严禁：搜索结果全量返回
results = await web_search(query)
return str(results)  # 所有结果+元数据
```

### ❌ 禁止：历史消息无限增长

```python
# ❌ 严禁：不做任何压缩地传递完整历史
messages = await load_all_messages(conversation_id)
response = await llm.create(messages=messages)  # 可能 100K+ tokens

# ❌ 严禁：保留所有历史图片的 base64
# 每张图片约 1000-5000 tokens，10 张图 = 10K-50K tokens
```

### ❌ 禁止：Skill 输出不受控

```python
# ❌ 严禁：Skill 返回不限大小的结果
class BadSkill:
    async def execute(self) -> str:
        data = await process()
        return json.dumps(data)  # 大小未知，可能爆上下文
```

---

## 十、检查清单

### 开发时自检

- [ ] 任何写入上下文的数据是否超过 3000 字符？如果是，是否走了 scratchpad？
- [ ] 工具结果是否做了摘要提取？还是全量返回？
- [ ] 图片/视频/音频是否转为文字描述 + 文件引用？
- [ ] Skill 输出是否声明了 `max_context_output`？
- [ ] Injector 是否有 token 预算控制？
- [ ] 历史消息中旧的 tool_result 是否折叠？
- [ ] 大文件是否分层处理（直接/摘要/路径引用）？

### 代码审查要点

1. 搜索 `content = await` 后是否有大小检查
2. 搜索 `tool_result` 相关代码是否有压缩逻辑
3. 搜索 `json.dumps` 返回是否有截断
4. 搜索 `message.append` 是否有 token 控制
5. 确认每个 Skill 的 `execute()` 返回值大小可控
6. 确认 Injector 的 `inject()` 有 MAX_TOKENS 常量

---

## 十一、与现有模块的协作关系

```
本规范                          现有模块
────────                        ────────
数据分层模型 ←──────────────── core/context/compaction/ (L2 裁剪)
                                core/context/compaction/tool_result.py (工具压缩)

Scratchpad 机制 ←─────────── workspace/storage/ (部分已有)
                                utils/file_processor.py (文件处理)

多模态处理 ←───────────────── utils/file_processor.py (图片/文本/文档分类)
                                core/context/compaction/__init__.py (_strip_old_images)

注入预算控制 ←─────────────── core/context/injectors/ (Phase 注入器)
                                core/context/context_engineering.py (KV-Cache)

对话历史压缩 ←─────────────── core/context/compaction/ (trim/compress_with_summary)
                                core/context/compaction/summarizer.py (摘要生成)

Skill 上下文纪律 ←─────────── instances/*/skills/ (SKILL.md)
                                core/skill/dynamic_loader.py (Skill 加载)
```

---

## 十二、当前框架差距分析与优化路径

### 已有优势（保持）

| 能力 | 实现 | 评价 |
|---|---|---|
| 工具结果压缩 | `ToolResultCompressor` (5000 字符阈值) | 良好，阈值可调低 |
| 双阈值消息裁剪 | `trim_by_token_budget` / `compress_with_summary` | 良好 |
| 旧 tool_result 截断 | `_compress_old_tool_results` (head 200 + tail 100) | 良好 |
| KV-Cache 优化 | `CacheOptimizer` (稳定 JSON 序列化) | 良好 |
| 注入器分层 | Phase 1/2/3 Injector 系统 | 良好 |

### 需要优化（P0）

| 差距 | 当前行为 | 目标行为 | 涉及文件 |
|---|---|---|---|
| 文本文件全塞上下文 | ≤ 50KB 全部嵌入消息 | ≤ 2KB 直接嵌入，> 2KB 走 scratchpad | `utils/file_processor.py` |
| 搜索结果不做提取 | 工具原样返回 JSON | 代码层 top-5 提取 + scratchpad | `tools/` 中的搜索工具 |
| Scratchpad 机制缺失 | 仅有 tool_results 存储 | 统一 scratchpad 目录 + RuntimeContext 方法 | `core/context/runtime.py` |
| Injector 无预算控制 | 注入大小不受限 | 每个 Injector 声明 MAX_TOKENS | `core/context/injectors/` |
| 历史图片粗暴丢弃 | 替换为 "[图片已省略]" | 替换为图片描述文字 | `core/context/compaction/` |

### 需要优化（P1）

| 差距 | 当前行为 | 目标行为 | 涉及文件 |
|---|---|---|---|
| 无 Context Folding | 旧轮次仅截断 | 子任务完成后折叠为结论 | `core/context/compaction/` |
| Skill 输出不受控 | 无输出大小约束 | SKILL.md 声明 + 运行时截断 | `core/skill/`, `instances/*/skills/` |
| 工具压缩阈值过高 | 5000 字符才触发 | 3000 字符触发即时压缩 | `core/context/compaction/tool_result.py` |
| Memory 召回无预算 | 返回所有匹配结果 | top-3 + 500 tokens 上限 | `core/memory/xiaodazi_memory.py` |
