---
description: "通用开发规范：中文注释、命名规范、代码风格"
alwaysApply: true
---

# 通用开发规范

## Python 版本
- 项目使用 **Python 3.12+**
- 可使用 3.12 新特性（如类型参数语法 `type X = ...`、`@override` 装饰器等）

## 异步编程规范
- 项目基于 **异步框架（FastAPI + asyncio）**
- **禁止**在异步函数中使用同步阻塞操作：
  - ❌ `with open()` → ✅ `async with aiofiles.open()`
  - ❌ `time.sleep()` → ✅ `await asyncio.sleep()`
  - ❌ `requests.get()` → ✅ `await httpx.AsyncClient().get()` 或 `aiohttp`
  - ❌ `json.load(file)` → ✅ `json.loads(await file.read())`
  - ❌ `shutil.copy()` → ✅ 使用 `aiofiles` 或封装异步版本
- 文件 I/O 必须使用 `aiofiles` 库
- HTTP 请求必须使用 `httpx` 或 `aiohttp`

## 语言规范
- 所有代码注释使用**中文**
- 所有文档使用**中文**编写
- Git commit 信息使用中文

## 命名规范
- 变量名、函数名：`snake_case`
- 类名：`PascalCase`
- 常量：`UPPER_CASE`
- 文件名：`snake_case.py`

## 代码风格
- 使用 4 个空格缩进
- 每行最大长度 100 字符
- 函数之间空 2 行
- 类方法之间空 1 行
- 运算符两侧加空格

## 导入顺序
```python
# 1. 标准库
import os
import sys
from typing import List, Dict

# 2. 第三方库
from fastapi import APIRouter
from pydantic import BaseModel

# 3. 本地模块
from core.agent import Agent
from models.chat import ChatMessage
```

## 类型注解
所有函数必须包含类型注解：

```python
from typing import List, Dict, Optional, Union

def process_data(
    items: List[str],
    config: Optional[Dict[str, Any]] = None
) -> Dict[str, Union[str, int]]:
    """使用类型注解提高代码可读性"""
    pass
```

## 文档字符串
使用 Google 风格的 docstring：

```python
def function(arg1: str, arg2: int) -> bool:
    """
    函数简短描述
    
    Args:
        arg1: 参数1的说明
        arg2: 参数2的说明
        
    Returns:
        返回值说明
        
    Raises:
        ValueError: 什么情况下抛出
    """
    pass
```

