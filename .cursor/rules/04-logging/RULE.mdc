---
description: "日志规范"
alwaysApply: true
---

# 日志规范

## 核心原则
- 使用 `from logger import get_logger` 获取日志记录器
- 在请求入口设置上下文（request_id/session_id/user_id）便于追踪
- 控制台输出易读格式，文件输出 JSON 格式
- 错误单独记录到 `logs/error.log`

## 基础使用
```python
from logger import get_logger

logger = get_logger()  # 自动获取模块名

logger.debug("调试信息")           # 仅开发环境
logger.info("正常操作")
logger.warning("警告")
logger.error("错误", exc_info=True)  # 记录异常堆栈
logger.critical("严重错误")
```

## 上下文追踪
```python
from logger import set_request_context

# 请求开始时设置
set_request_context(
    request_id="req-123",
    session_id="sess-456",
    user_id="user-789"
)

# 后续日志自动包含上下文
logger.info("处理请求")
# 输出: 2024-01-01 12:00:00 [INFO] [req-123] [sess-456] [user-789] module.py:42: 处理请求
```

## 结构化日志
```python
# 添加额外字段
logger.info("操作完成", extra={
    "duration_ms": 123,
    "operation": "db_query"
})
```

## 性能监控
```python
from logger import log_execution_time

with log_execution_time("数据库查询"):
    results = await db.query(...)
```

## 最佳实践
- 包含上下文信息：`logger.info(f"处理任务 task_id={task_id}")`
- 记录异常必须 `exc_info=True`
- 记录关键操作的开始和结束
- 使用 extra 添加性能指标

## 日志文件
- `logs/app.log`：所有日志（JSON 格式）
- `logs/error.log`：仅错误日志（JSON 格式）
- 控制台：彩色易读格式

## 查看日志
```bash
tail -f logs/app.log              # 最新日志
tail -f logs/error.log            # 错误日志
grep "req-123" logs/app.log | jq  # 搜索请求
```

## 禁止事项
- ❌ 记录敏感信息（密码、token、API key）
- ❌ 在循环中过度记录
- ❌ 使用 `print()` 代替日志
- ❌ 吞异常不记录
