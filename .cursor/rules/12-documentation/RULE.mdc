---
description: "文档编写规范：聚焦结构、示例、同步维护（避免冗长模板）"
globs:
  - "docs/**/*.md"
  - "README.md"
  - "*.md"
alwaysApply: True
---

# 文档编写规范

## 核心原则
- **写给未来的同事看**：先说明“是什么/为什么/怎么用”
- **可复制**：命令、配置、代码块要可直接复制执行
- **少而精**：模板只保留最常用结构；不要堆满占位内容
- **保持同步**：接口、行为变更时同步更新文档

## 文档结构（推荐）

### README 必备章节
- 项目一句话介绍
- 快速开始（安装、启动、最小可运行示例）
- 配置（必须说明从哪里读配置、关键环境变量）
- 常见问题（只收录真实踩坑）

### 设计/协议类文档（docs/）推荐章节
- 背景与目标
- 术语
- 协议/接口约定（含错误码/事件）
- 示例（至少 1 个完整例子）
- 兼容性/迁移（若有破坏性变更）

## 代码块规范
- 命令：使用 `bash` 代码块
- 配置：使用 `yaml` / `json` 代码块
- Python：示例必须包含中文注释（解释关键步骤）
- 避免无意义占位（如“详细内容...”）

## 示例（精简模板）

### README 快速开始（示例）

```markdown
## 快速开始

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python main.py
```

验证服务是否正常：

```bash
python -m pytest -q
```
```

### API/协议说明（示例）

```markdown
## 发送聊天消息

**端点**：`POST /chat`

**请求体**：
```json
{ "message": "你好", "session_id": "sess-123" }
```

**成功响应**：
```json
{ "success": true, "response": "..." }
```

**错误响应**（示例）：
```json
{ "success": false, "error": "缺少 session_id" }
```
```

## 引用与链接
- 站内文件引用使用相对路径（例如 `docs/00-ARCHITECTURE-OVERVIEW.md`）
- 链接文本要有意义（不要只写“点击这里”）

## 维护约定
- 破坏性变更必须：更新相关文档 + 在文档中写明迁移方式
- 文档标题与文件名保持一致、可搜索
- 同一主题不要在多个文档中重复粘贴（优先链接到单一事实来源）
