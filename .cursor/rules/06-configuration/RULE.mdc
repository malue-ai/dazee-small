---
description: "配置文件规范"
globs:
  - "config/**/*.yaml"
  - "config/**/*.yml"
alwaysApply: false
---

# 配置文件规范

## 核心原则
- 配置清晰可读，层级清晰
- 敏感信息必须使用环境变量
- 启动时验证配置，失败报中文错误

## YAML 编写规范
- 使用 2 空格缩进
- 键名使用 `snake_case`
- 相关配置分组
- 层级 <= 4 层

### 推荐结构

```yaml
service_name: "ZenFlux Agent"

server:
  host: "0.0.0.0"
  port: 8000

llm:
  provider: "openai"
  model: "gpt-4"
  temperature: 0.7
  api_key: ${OPENAI_API_KEY}  # 从环境变量读取

logging:
  level: "INFO"
  file: "logs/app.log"
```

## 环境变量
- 所有密钥使用 `${ENV_VAR}` 或 `os.getenv()` 读取
- `.env` 仅用于本地开发

### .env 示例

```bash
OPENAI_API_KEY=sk-...
LOG_LEVEL=INFO
APP_ENV=development
```

## 配置校验

启动时验证配置：

```python
def validate_config(cfg: dict) -> None:
    """启动前配置校验"""
    llm = cfg.get("llm", {})
    if not llm.get("provider"):
        raise ValueError("缺少配置: llm.provider")
    
    port = cfg.get("server", {}).get("port")
    if not isinstance(port, int) or not (1 <= port <= 65535):
        raise ValueError("配置错误: server.port 必须是 1-65535 的整数")
```

## 禁止事项
- ❌ YAML 中硬编码 API key、token、密码
- ❌ 生产和开发配置混在一起
- ❌ 使用含糊键名（如 `x1`, `cfg2`）
