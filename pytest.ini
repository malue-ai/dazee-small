[pytest]
# pytest 配置

# asyncio 模式：auto 自动处理 async 函数
asyncio_mode = auto

# 测试文件匹配模式
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 添加项目根目录到 Python 路径
pythonpath = .

# 默认参数
addopts = -v --tb=short
