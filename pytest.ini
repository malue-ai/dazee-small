[pytest]
# pytest 配置

# asyncio 模式：auto 自动处理 async 函数
asyncio_mode = auto

# 测试文件匹配模式
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 添加项目根目录到 Python 路径
pythonpath = .

# pytest-dotenv: 自动加载 .env 文件（在任何测试代码之前）
env_files = .env

# 默认参数
addopts = -v --tb=short
