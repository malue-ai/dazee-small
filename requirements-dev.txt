# ============================================================
# 开发 / 测试 / 构建工具
#
# 安装方式：pip install -r requirements-dev.txt
# 注意：会自动安装 requirements.txt 中的运行时依赖
# ============================================================

-r requirements.txt

# 测试框架
pytest>=8.0.0
pytest-asyncio>=0.23.0

# 打包工具
pyinstaller==6.11.1
